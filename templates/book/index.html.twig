{% extends 'base.html.twig' %}

{% block title %}Catalogue des livres{% endblock %}

{% block body %}
<div class="container">
    <div class="d-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0"><i class="bi bi-journal-text me-2 text-primary"></i>Catalogue des livres</h1>
        <a href="{{ path('app_book_new') }}" class="btn btn-primary">
            <i class="bi bi-plus-lg me-1"></i>Ajouter un livre
        </a>
    </div>

    {{ component('BookSearch') }}
</div>
{% endblock %}
