{% extends 'base.html.twig' %}

{% block title %}Emprunts{% endblock %}

{% block body %}
<div class="container">
    <div class="d-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0"><i class="bi bi-arrow-left-right me-2 text-warning"></i>Emprunts</h1>
        <a href="{{ path('app_loan_new') }}" class="btn btn-success">
            <i class="bi bi-plus-lg me-1"></i>Nouvel emprunt
        </a>
    </div>

    {{ component('LoanSearch') }}
</div>
{% endblock %}
